function [x, P] = simple_model(x, P, T, Ralpha)

Sw = Somega (omega);
Gq = Sq(x);
F = 0.5*Sw*T + eye(4);
G = (0.5*Sw*T + eye(4))*T*0.5*Gq;

x = F*x;
P = F*P*F' + G*Rw*G';

[x, P] = mu_normalizeQ(x, P);   %normalize
end